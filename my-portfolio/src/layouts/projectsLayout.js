import React from 'react';

function ProjectsLayout ({children}) {
    return (
        <div>
            <div>
                {children}
                {children}
                {children}
            </div>
            <div>
                {children}
                {children}
                {children}
            </div>
        </div>
    )
};

export default ProjectsLayout;